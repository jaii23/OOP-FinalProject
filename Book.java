public class Book extends Material {
    public Book(int id, String materialType, String title, String author, int year, String pub, int copies) {
        super(id, materialType, title, author, year, pub, copies);
    }
}